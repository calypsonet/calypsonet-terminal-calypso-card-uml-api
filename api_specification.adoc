= Terminal Calypso Card API -- Specification
Calypso Networks Association <https://calypsonet.org>
v3.0.0-SNAPSHOT, {docdate}
:doctype: book
:toc: left
:toclevels: 4
:sectnums:
:sectnumlevels: 4
:sectanchors:
:sectlinks:
:data-uri:
:icons: font
:rouge-style: github
:source-highlighter: rouge
:docinfodir: {docdir}/assets
:docinfo: shared
:imagesoutdir: {docdir}/gen
:pdf-themesdir: {docdir}/assets
:pdf-theme: calypso
:pdf-fontsdir: {docdir}/assets/fonts
:experimental:
:xrefstyle: short
:figure-caption: Figure
:table-caption: Table
:example-caption: Example
:appendix-caption: Appendix
:organization: Calypso Networks Association
:short-name: CNA
:spec-name: Terminal Calypso Card API
:spec-id: CNA-TCC-API
:spec-version: 3.0.0-SNAPSHOT

[abstract]
== Abstract

This document is the official technical specification of the **{spec-name}** standardised
by the **{organization}** ({short-name}).
It defines, in a language-agnostic way, the interfaces, classes, enumerations, exceptions, structural
relationships and behavioural requirements that any conforming implementation of the {spec-name} MUST satisfy.

The {spec-name} is the *application-facing* surface used by terminal applications to operate Calypso transit and ticketing cards.
It builds on top of the *Terminal Reader API* (<<ref_cna_tr,CNA-TR-API>>) and delegates every cryptographic operation to a symmetric (<<ref_cna_tccs,CNA-TCCS-API>>) or asymmetric (<<ref_cna_tcca,CNA-TCCA-API>>) crypto module.

[discrete]
== Document Status

[cols="1h,3", grid=rows, frame=all]
|===
| Specification ID    | {spec-id}
| Version             | {spec-version}
| Editor              | {organization}
| Reference license   | Eclipse Public License -- v 2.0 (EPL-2.0)
|===

NOTE: The present document specifies the {spec-version} of the {spec-name}. It defines a single, coherent
baseline against which conforming implementations are evaluated; the *Since* column indicates the version in
which each member was introduced.

// =============================================================================
== Introduction

=== Purpose

The {spec-name} defines the public surface used by terminal applications to:

* select a Calypso card through a <<ref_cna_tr,CNA-TR-API>> selection scenario enriched with Calypso-specific commands;
* operate read-only transactions without involving a crypto module (*free* transaction);
* operate fully authenticated transactions using a *symmetric* crypto module (<<ref_cna_tccs,CNA-TCCS-API>>) -- either in the
*regular* mode (compatible with every Calypso product) or in the *extended* mode (additional features only available on Calypso Prime Extended);
* operate fully authenticated transactions using an *asymmetric* crypto module (<<ref_cna_tcca,CNA-TCCA-API>>) in *PKI* mode;
* expose a dynamic view of the card (`CalypsoCard`) updated throughout the transaction.

=== Scope

This specification covers:

* the factory used to instantiate the public types of the API (`CalypsoCardApiFactory`);
* the data model of a Calypso card (`CalypsoCard`, `ElementaryFile`, `FileData`, `FileHeader`, `DirectoryHeader`, Stored Value log records);
* the selection extension (`CalypsoCardSelectionExtension`);
* the family of transaction managers (Free, Secure Regular, Secure Extended, Secure PKI);
* the *symmetric* and *asymmetric* security setting carriers;
* the *Stored Value* operations and *Search Record Multiple* support;
* the SPIs implemented by *crypto modules* to plug into the secure transaction managers;
* the family of exceptions raised during a Calypso transaction.

The following topics are *out of scope*:

* the on-the-wire APDU dialogue with the card (handled internally by the implementation);
* the cryptographic algorithms themselves -- only the SPI contract is normative;
* the on-the-wire transport between the terminal and any remote ticketing system.

=== Conformance

A software product conforms to this specification if and only if:

. it implements every interface and class defined in <<sec_specification>> with the operations and semantics
  prescribed in this document,
. it raises exceptions exclusively of the types defined in <<sec_exceptions>> for the situations described,
. it preserves the structural and behavioural relationships described in <<sec_architecture>>.

Throughout this specification, the key words **MUST**, **MUST NOT**, **SHOULD**, **SHOULD NOT**, **RECOMMENDED**,
**MAY** and **OPTIONAL** are to be interpreted as described in **RFC 2119** and **RFC 8174** when, and only when,
they appear in all capitals.

In conformance with RFC 2119, **SHALL** is equivalent to **MUST**; this specification uses **MUST** exclusively in
order to avoid ambiguity.

=== Document Conventions

==== Naming

Type names follow `UpperCamelCase`; operation, parameter and enumeration-member names follow `lowerCamelCase`
except for enumeration values which use `UPPER_SNAKE_CASE`. Names defined by this specification are reproduced
as-is in the UML class diagram (<<sec_class_diagram>>).

==== Language-agnostic types

[cols="1,3,3", options="header"]
|===
| Symbolic type    | Description                                                     | Typical bindings
| `string`         | Sequence of characters, UTF-8 encoded by default.                | `String`, `string`, `str`.
| `boolean`        | Two-state truth value.                                           | `boolean`, `bool`.
| `byte`           | Signed 8-bit value.                                              | `byte`, `i8`.
| `short`          | Signed 16-bit integer.                                           | `short`, `Int16`.
| `integer`        | Signed 32-bit integer.                                           | `int`, `Int32`.
| `byte array`     | Ordered sequence of octets.                                      | `byte[]`, `[u8]`, `bytes`.
| `list<T>`        | Ordered collection of `T` values, may contain duplicates.        | `List<T>`, `Vec<T>`.
| `set<T>`         | Unordered collection of unique `T` values.                       | `Set<T>`, `HashSet<T>`.
| `map<K,V>`       | Associative array binding keys of type `K` to values of type `V`.| `Map<K,V>`, `Dictionary<K,V>`.
| `sortedMap<K,V>` | Associative array sorted by key.                                 | `SortedMap<K,V>`, `BTreeMap<K,V>`.
| `T?`             | Nullable value: either a `T` value or the absence of value (`null`). | `Integer`/`Short`/`Byte` (boxed), `Optional<T>`, `T?`.
| `void`           | Indicates that an operation returns no value.                    | `void`, `Unit`, `None`.
|===

==== Operation tables

Each operation defined in this document is described by a table of the form:

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | The operation's language-agnostic signature.
| Description | A normative description of the operation's behaviour.
| Parameters  | A description of each input parameter.
| Returns     | A description of the returned value, if any.
| Throws      | A list of exceptional conditions, each mapped to a specific exception type.
|===

==== Operation contracts

To avoid repetition, the following rules apply to every operation table in <<sec_specification>> and are therefore
not restated for each operation:

* *Non-`null` arguments.* Unless explicitly stated otherwise, every input parameter MUST be non-`null`. An
  implementation MUST raise `IllegalArgumentException` if a `null` value is supplied. This implicit `null` check is
  not repeated in the *Throws* row, which states `None.` when no other exception can occur.
* *Non-`null` results.* Unless the return type is marked nullable (`T?`) or the *Returns* row states otherwise, an
  operation returns a non-`null` value.
* *Collections.* Unless the return type is marked nullable (`T?`), an operation whose return type is a collection
  (e.g. `list`, `set`, `map`) returns an empty collection rather than `null`.
* *Fluent composition.* Builder-style operations return the current instance so that calls can be chained.

==== Concurrency

Unless explicitly stated otherwise, the stateful types defined by this specification are *not* thread-safe. A given
instance MUST be accessed from a single thread at a time; concurrent use of the same instance without external
synchronisation results in undefined behaviour. Distinct instances MAY be used concurrently.

=== References

[cols="1,1,4", options="header"]
|===
| Reference | Identifier | Title

| [[ref_rfc2119]] [RFC 2119]
| RFC 2119
| _Key words for use in RFCs to Indicate Requirement Levels_, S. Bradner, March 1997.

| [[ref_rfc8174]] [RFC 8174]
| RFC 8174
| _Ambiguity of Uppercase vs Lowercase in RFC 2119 Key Words_, B. Leiba, May 2017.

| [[ref_iso7816_4]] [ISO/IEC 7816-4]
| ISO/IEC 7816-4
| _Identification cards -- Integrated circuit cards -- Part 4: Organization, security and commands for interchange_.

| [[ref_cna_tr]] [CNA-TR-API]
| CNA-TR-API
| _Terminal Reader API Specification_, {organization}.

| [[ref_cna_tccs]] [CNA-TCCS-API]
| CNA-TCCS-API
| _Terminal Calypso Crypto Symmetric API Specification_, {organization}.

| [[ref_cna_tcca]] [CNA-TCCA-API]
| CNA-TCCA-API
| _Terminal Calypso Crypto Asymmetric API Specification_, {organization}.

| [[ref_cna_tccl]] [CNA-TCCL-API]
| CNA-TCCL-API
| _Terminal Calypso Crypto Legacy SAM API Specification_, {organization}.
|===

// =============================================================================
== Terms, Definitions and Abbreviations

=== Terms and Definitions

[cols="1,4", options="header"]
|===
| Term              | Definition

| Calypso Card      | Smart card compliant with the Calypso specifications.
| Secure Session    | Authenticated and integrity-protected exchange between the terminal and the card.
| Regular Mode      | Secure session mode compatible with every Calypso product.
| Extended Mode     | Secure session mode supporting additional features (early mutual authentication, encryption of session APDUs, larger sessions). Available on Calypso Prime Extended.
| PKI Mode          | Secure session mode using asymmetric cryptography (Calypso 3.3+).
| Stored Value (SV) | Calypso feature exposing a card-resident monetary counter.
| Crypto Module     | Component implementing the SPI exposed by <<ref_cna_tccs,CNA-TCCS-API>> or <<ref_cna_tcca,CNA-TCCA-API>>.
| KIF / KVC         | Calypso Key Identifier / Key Version Code.
| HCE               | Host Card Emulation.
|===

=== Abbreviations

[cols="1,4", options="header"]
|===
| Abbreviation | Expansion

| AID    | Application IDentifier
| APDU   | Application Protocol Data Unit
| CNA    | Calypso Networks Association
| DF     | Dedicated File
| EF     | Elementary File
| FCI    | File Control Information
| FCP    | File Control Parameters
| HCE    | Host Card Emulation
| KIF    | Key Identifier
| KVC    | Key Version Code
| LID    | Long IDentifier (file id)
| MAC    | Message Authentication Code
| PIN    | Personal Identification Number
| PKI    | Public Key Infrastructure
| SAM    | Secure Application Module
| SFI    | Short File Identifier
| SPI    | Service Provider Interface
| SV     | Stored Value
| UML    | Unified Modelling Language
|===

// =============================================================================
[[sec_architecture]]
== Architectural Overview

=== Functional positioning

[plantuml,target=architecture-overview,format=svg]
....
@startuml
skinparam shadowing false
skinparam roundCorner 8

component "Terminal Application" as App
component "Terminal Reader API\n[CNA-TR-API]" as TR
component "Terminal Calypso Card API\n(this specification)" as TCC
component "Terminal Calypso Crypto\nSymmetric / Asymmetric APIs\n[CNA-TCCS-API] / [CNA-TCCA-API]" as TCCx
component "Crypto Module\n(SAM, PKI library)" as Mod
component "Reader / Card" as RC

App  --> TR  : selection / observation
App  --> TCC : transactions
TCC  --> TCCx: delegates crypto
TCC  --> TR  : reuses
TCCx --> Mod : implemented by
TR   --> RC  : drives
@enduml
....

=== Logical packages

[cols="1,1,4", options="header"]
|===
| Package                                  | Role     | Summary

| `calypso.card`                           | Public API
| Factory, properties, enumerations (`GetDataTag`, `PutDataTag`, `SelectFileControl`, `WriteAccessLevel`).

| `calypso.card.card`                      | Public API
| Data model of a Calypso card: `CalypsoCard`, files, headers, SV logs and selection extension.

| `calypso.card.transaction`               | Public API
| Transaction managers (Free / Secure Regular / Secure Extended / Secure PKI), security setting carriers, `SearchCommandData`, `SvAction`, `SvOperation` and the family of exceptions.

| `calypso.card.transaction.spi`           | SPI
| Marker interfaces implemented by crypto extensions (`SymmetricCryptoCardTransactionManagerFactory`, `AsymmetricCryptoCardTransactionManagerFactory`, certificate markers, `CardTransactionCryptoExtension`).
|===

[[sec_class_diagram]]
=== UML class diagram

The following UML class diagram provides a visual representation of the types and relationships defined by this specification:

[plantuml,target="api_class_diagram",format="svg"]
----
include::api_class_diagram.puml[Terminal Calypso Card API class diagram,align=center]
----

// =============================================================================
[[sec_specification]]
== API Specification

Stateful operations are subject to the ordering and lifecycle constraints defined in <<sec_behaviours>>.

The API is organized into the following packages:

[[sec_pkg_calypso_card]]

[[sec_pkg_calypso_card_card]]

[[sec_pkg_calypso_card_transaction]]

[[sec_pkg_calypso_card_transaction_spi]]

=== Classes

[[type_CalypsoCardApiProperties]]
==== Calypso Card API Properties

[cols="1h,4", grid=rows, frame=all]
|===
| Kind     | Final class
| Namespace  | `calypso.card`
| Since    | 1.0
| Purpose  | Exposes the immutable properties of the {spec-name}.
|===

===== Constants

[cols="1,1,1,4", options="header"]
|===
| Name      | Type     | Since | Description
| `VERSION` | `string` | 1.0 | String representation of the API version (e.g. `"3.0"`).
|===

=== API Interfaces

[[type_AsymmetricCryptoSecuritySetting]]
==== Asymmetric Crypto Security Setting

[cols="1h,4", grid=rows, frame=all]
|===
| Kind     | Interface
| Namespace  | `calypso.card.transaction`
| Since    | 2.1
| Purpose  | Security setting carrier for PKI Calypso card transactions.
|===

[[op_AsymmetricCryptoSecuritySetting_addPcaCertificate]]
[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `addPcaCertificate(pcaCertificate: PcaCertificate) -> AsymmetricCryptoSecuritySetting`
| Since       | 2.1
| Description
| Registers a self-signed PCA certificate. The implementation MUST verify the self-signature, the validity period
and any constraints.
| Returns     | A `AsymmetricCryptoSecuritySetting`.
| Throws      | `IllegalArgumentException` if invalid; `IllegalStateException` if already registered; `InvalidCertificateException` if the verification fails.
|===

[[op_AsymmetricCryptoSecuritySetting_addCaCertificate]]
[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `addCaCertificate(caCertificate: CaCertificate) -> AsymmetricCryptoSecuritySetting`
| Since       | 2.1
| Description | Registers a CA certificate. The issuer's certificate MUST be loaded first.
| Returns     | A `AsymmetricCryptoSecuritySetting`.
| Throws      | None.
|===

[[op_AsymmetricCryptoSecuritySetting_addCaCertificateParser]]
[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `addCaCertificateParser(caCertificateParser: CaCertificateParser) -> AsymmetricCryptoSecuritySetting`
| Since       | 2.1
| Description
| Registers a CA certificate parser, used when the CA certificate is not already available.
| Returns     | A `AsymmetricCryptoSecuritySetting`.
| Throws      | `IllegalStateException` if a parser is already registered for the same certificate type.
|===

[[op_AsymmetricCryptoSecuritySetting_addCardCertificateParser]]
[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `addCardCertificateParser(cardCertificateParser: CardCertificateParser) -> AsymmetricCryptoSecuritySetting`
| Since       | 2.1
| Description | Registers a card certificate parser. Same constraint as above.
| Returns     | A `AsymmetricCryptoSecuritySetting`.
| Throws      | None.
|===

[[op_AsymmetricCryptoSecuritySetting_assignOpenSecureSessionMaxDuration_byDf]]
[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `assignOpenSecureSessionMaxDuration(csnMin: long, dfName: byte array, maxDuration: long) -> AsymmetricCryptoSecuritySetting`
| Since       | 3.0
| Description
| Sets the maximum duration (in milliseconds) of an open PKI secure session for cards whose CSN is >= `csnMin` and
whose application DF matches `dfName`. See <<sec_csnmin>>.
| Returns     | A `AsymmetricCryptoSecuritySetting`.
| Throws      | None.
|===

[[op_AsymmetricCryptoSecuritySetting_assignOpenSecureSessionMaxDuration_allDf]]
[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `assignOpenSecureSessionMaxDuration(csnMin: long, maxDuration: long) -> AsymmetricCryptoSecuritySetting`
| Since       | 3.0
| Description | Same as above, applied to every DF.
| Returns     | A `AsymmetricCryptoSecuritySetting`.
| Throws      | None.
|===

[[type_CalypsoCard]]
==== Calypso Card

[cols="1h,4", grid=rows, frame=all]
|===
| Kind     | Interface
| Namespace  | `calypso.card.card`
| Extends  | `IsoSmartCard` (<<ref_cna_tr,CNA-TR-API>>)
| Since    | 1.0
| Purpose  | Dynamic view of the card's content, updated from selection to the end of the transaction.
|===

A `CalypsoCard` carries: application identification (revision, class, DF name, serial number, ATR, issuer), the indication of optional features (Stored Value, PIN, Rev 3.2 mode, ratification management), the management information of the modification buffer, the invalidation status and the files / counters / SV data read or modified during the transaction.

===== Get Directory Header

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `getDirectoryHeader() -> DirectoryHeader?`
| Since       | 1.0
| Description | Metadata of the current DF; `null` if not set.
| Returns     | A `DirectoryHeader`.
| Throws      | None.
|===

[[op_CalypsoCard_getFileBySfi]]
[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `getFileBySfi(sfi: byte) -> ElementaryFile?`
| Since       | 1.0
| Description | EF whose SFI matches; `null` if not found, or if `sfi == 0`.
| Returns     | A `ElementaryFile`.
| Throws      | None.
|===

[[op_CalypsoCard_getFileByLid]]
[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `getFileByLid(lid: short) -> ElementaryFile?`
| Since       | 1.0
| Description | EF whose LID matches; `null` if not found.
| Returns     | A `ElementaryFile`.
| Throws      | None.
|===

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `getFiles() -> set<ElementaryFile>`
| Since       | 1.1
| Description | All known EFs of the current DF. During a secure session, this set MUST reflect every in-session modification (which is rolled back on session failure).
| Returns     | A `set<ElementaryFile>`.
| Throws      | None.
|===

===== Get Product Type

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `getProductType() -> ProductType`
| Since       | 1.0
| Description | Returns the card product type (`PRIME_REVISION_1` / `PRIME_REVISION_2` / `PRIME_REVISION_3` / `LIGHT` / `BASIC` / `UNKNOWN`).
| Returns     | A `ProductType`.
| Throws      | None.
|===

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `isHce() -> boolean`
| Since       | 1.0
| Description | Indicates whether the card is a Calypso HCE.
| Returns     | A `boolean` value.
| Throws      | None.
|===

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `isDfInvalidated() -> boolean`
| Since       | 1.0
| Description | Tells whether the current DF is invalidated.
| Returns     | A `boolean` value.
| Throws      | None.
|===

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `getDfName() -> byte array`
| Since       | 1.0
| Description | DF name (5 to 16 bytes), as per ISO/IEC 7816-4.
| Returns     | A `byte array`.
| Throws      | None.
|===

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `getApplicationSerialNumber() -> byte array`
| Since       | 1.0
| Description | Application Serial Number (8 bytes).
| Returns     | A `byte array`.
| Throws      | None.
|===

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `getStartupInfoRawData() -> byte array`
| Since       | 1.0
| Description | Raw Calypso startup information.
| Returns     | A `byte array`.
| Throws      | None.
|===

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `getPlatform() -> byte`
| Since       | 1.0
| Description | Platform identification byte (chip reference).
| Returns     | A `byte` value.
| Throws      | None.
|===

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `getApplicationType() -> byte`
| Since       | 1.0
| Description | Application Type byte (product type + options).
| Returns     | A `byte` value.
| Throws      | None.
|===

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `getApplicationSubtype() -> byte`
| Since       | 1.0
| Description | Application Subtype byte (file structure reference).
| Returns     | A `byte` value.
| Throws      | None.
|===

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `getSoftwareIssuer() -> byte`
| Since       | 1.0
| Description | Software Issuer byte.
| Returns     | A `byte` value.
| Throws      | None.
|===

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `getSoftwareVersion() -> byte`
| Since       | 1.0
| Description | Software Version byte.
| Returns     | A `byte` value.
| Throws      | None.
|===

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `getSoftwareRevision() -> byte`
| Since       | 1.0
| Description | Software Revision byte.
| Returns     | A `byte` value.
| Throws      | None.
|===

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `getSessionModification() -> byte`
| Since       | 1.0
| Description | Session Modification byte (depending on the card, max bytes modifiable or max write commands per session).
| Returns     | A `byte` value.
| Throws      | None.
|===

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `getTraceabilityInformation() -> byte array`
| Since       | 1.1
| Description | Traceability information (software issuer ID and discretionary data); empty if not available.
| Returns     | A `byte array`.
| Throws      | None.
|===

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `getCardPublicKey() -> byte array`
| Since       | 2.1
| Description | Card public key; empty if not available.
| Returns     | A `byte array`.
| Throws      | None.
|===

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `getCardCertificate() -> byte array`
| Since       | 2.1
| Description | Card certificate; empty if not available.
| Returns     | A `byte array`.
| Throws      | None.
|===

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `getCaCertificate() -> byte array`
| Since       | 2.1
| Description | CA certificate; empty if not available.
| Returns     | A `byte array`.
| Throws      | None.
|===

===== Is DF Ratified

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `isDfRatified() -> boolean`
| Since       | 1.0
| Description | Tells whether the last session has been ratified.
| Returns     | A `boolean` value.
| Throws      | None.
|===

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `getTransactionCounter() -> integer`
| Since       | 1.2
| Description | Transaction counter from the output of the last successful *Open Secure Session*. *Note:* commands such as *Change Key*, *Change/Verify PIN*, *SV Debit/Undebit/Reload* decrement the card counter but MUST NOT update this returned value.
| Returns     | An `integer` value.
| Throws      | None.
|===

===== Is PKI Mode Supported

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `isPkiModeSupported() -> boolean`
| Since       | 1.0
| Description | Indicates whether Public Key Authentication is supported (Rev 3.3+).
| Returns     | A `boolean` value.
| Throws      | None.
|===

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `isExtendedModeSupported() -> boolean`
| Since       | 1.0
| Description | Indicates whether the Extended Mode is supported (Rev 3.2+). MAY be updated after session opening depending on the type of key used.
| Returns     | A `boolean` value.
| Throws      | None.
|===

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `isRatificationOnDeselectSupported() -> boolean`
| Since       | 1.0
| Description | Indicates whether ratification happens on deselect (ratification command not required).
| Returns     | A `boolean` value.
| Throws      | None.
|===

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `isPinFeatureAvailable() -> boolean`
| Since       | 1.0
| Description | Indicates whether the Calypso PIN feature is available.
| Returns     | A `boolean` value.
| Throws      | None.
|===

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `isPinBlocked() -> boolean`
| Since       | 1.0
| Description | Indicates whether the PIN is blocked.
| Returns     | A `boolean` value.
| Throws      | `IllegalStateException` if the PIN has not been checked.
|===

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `getPinAttemptRemaining() -> integer`
| Since       | 1.0
| Description | Number of erroneous PIN presentations remaining.
| Returns     | An `integer` value.
| Throws      | `IllegalStateException` if the PIN has not been checked.
|===

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `isSvFeatureAvailable() -> boolean`
| Since       | 1.0
| Description | Indicates whether the SV feature is available.
| Returns     | A `boolean` value.
| Throws      | None.
|===

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `getSvBalance() -> integer`
| Since       | 1.0
| Description | Current SV balance.
| Returns     | An `integer` value.
| Throws      | `IllegalStateException` if no SV Get has been executed.
|===

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `getSvLastTNum() -> integer`
| Since       | 1.0
| Description | Last SV transaction number. Same precondition as above.
| Returns     | An `integer` value.
| Throws      | None.
|===

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `getSvLoadLogRecord() -> SvLoadLogRecord?`
| Since       | 1.0
| Description | Last SV load log record; `null` if not available.
| Returns     | A `SvLoadLogRecord`.
| Throws      | None.
|===

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `getSvDebitLogLastRecord() -> SvDebitLogRecord?`
| Since       | 1.0
| Description | Last SV debit log record; `null` if not available.
| Returns     | A `SvDebitLogRecord`.
| Throws      | None.
|===

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `getSvDebitLogAllRecords() -> list<SvDebitLogRecord>`
| Since       | 1.0
| Description | All SV debit log records read from the card.
| Returns     | A `list<SvDebitLogRecord>`.
| Throws      | None.
|===

[[type_CalypsoCardApiFactory]]
==== Calypso Card API Factory

[cols="1h,4", grid=rows, frame=all]
|===
| Kind     | Interface
| Namespace  | `calypso.card`
| Since    | 2.0
| Purpose  | Factory used to obtain instances of every public type provided by the API.
|===

[[op_CalypsoCardApiFactory_createAsymmetricCryptoSecuritySetting]]
===== Create Asymmetric Crypto Security Setting

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `createAsymmetricCryptoSecuritySetting(cryptoCardTransactionManagerFactory: AsymmetricCryptoCardTransactionManagerFactory) -> AsymmetricCryptoSecuritySetting`
| Since       | 2.1
| Description | Returns a new <<type_AsymmetricCryptoSecuritySetting,`AsymmetricCryptoSecuritySetting`>>.
| Parameters  | `cryptoCardTransactionManagerFactory` -- the factory of the crypto card transaction manager to be used.
| Returns     | A <<type_AsymmetricCryptoSecuritySetting,`AsymmetricCryptoSecuritySetting`>>.
| Throws      | `IllegalArgumentException` -- if the factory is invalid.
|===

===== Create Calypso Card Selection Extension

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `createCalypsoCardSelectionExtension() -> CalypsoCardSelectionExtension`
| Since       | 2.0
| Description | Returns a new <<type_CalypsoCardSelectionExtension,`CalypsoCardSelectionExtension`>>.
| Returns     | A <<type_CalypsoCardSelectionExtension,`CalypsoCardSelectionExtension`>>.
| Throws      | None.
|===

[[op_CalypsoCardApiFactory_createFreeTransactionManager]]
===== Create Free Transaction Manager

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `createFreeTransactionManager(cardReader: CardReader, card: CalypsoCard) -> FreeTransactionManager`
| Since       | 2.0
| Description | Returns a new <<type_FreeTransactionManager,`FreeTransactionManager`>>.
| Parameters
| `cardReader` -- the card reader to be used. +
`card` -- the selected card on which to operate the transaction.
| Returns     | A <<type_FreeTransactionManager,`FreeTransactionManager`>>.
|===

===== Create Search Command Data

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `createSearchCommandData() -> SearchCommandData`
| Since       | 2.0
| Description | Returns a new <<type_SearchCommandData,`SearchCommandData`>>.
| Returns     | A <<type_SearchCommandData,`SearchCommandData`>>.
| Throws      | None.
|===

[[op_CalypsoCardApiFactory_createSecureExtendedModeTransactionManager]]
===== Create Secure Extended Mode Transaction Manager

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `createSecureExtendedModeTransactionManager(cardReader: CardReader, card: CalypsoCard, securitySetting: SymmetricCryptoSecuritySetting) -> SecureExtendedModeTransactionManager`
| Since       | 2.0
| Description | Returns a new <<type_SecureExtendedModeTransactionManager,`SecureExtendedModeTransactionManager`>>.
| Parameters
| `cardReader` -- the card reader to be used. +
`card` -- the selected card on which to operate the transaction. +
`securitySetting` -- the security setting to be used.
| Returns     | A <<type_SecureExtendedModeTransactionManager,`SecureExtendedModeTransactionManager`>>.
|===

[[op_CalypsoCardApiFactory_createSecurePkiModeTransactionManager]]
===== Create Secure PKI Mode Transaction Manager

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `createSecurePkiModeTransactionManager(cardReader: CardReader, card: CalypsoCard, securitySetting: AsymmetricCryptoSecuritySetting) -> SecurePkiModeTransactionManager`
| Since       | 2.1
| Description | Returns a new <<type_SecurePkiModeTransactionManager,`SecurePkiModeTransactionManager`>>.
| Parameters
| `cardReader` -- the card reader to be used. +
`card` -- the selected card on which to operate the transaction. +
`securitySetting` -- the security setting to be used.
| Returns     | A <<type_SecurePkiModeTransactionManager,`SecurePkiModeTransactionManager`>>.
|===

[[op_CalypsoCardApiFactory_createSecureRegularModeTransactionManager]]
===== Create Secure Regular Mode Transaction Manager

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `createSecureRegularModeTransactionManager(cardReader: CardReader, card: CalypsoCard, securitySetting: SymmetricCryptoSecuritySetting) -> SecureRegularModeTransactionManager`
| Since       | 2.0
| Description | Returns a new <<type_SecureRegularModeTransactionManager,`SecureRegularModeTransactionManager`>>.
| Parameters
| `cardReader` -- the card reader to be used. +
`card` -- the selected card on which to operate the transaction. +
`securitySetting` -- the security setting to be used.
| Returns     | A <<type_SecureRegularModeTransactionManager,`SecureRegularModeTransactionManager`>>.
|===

[[op_CalypsoCardApiFactory_createSymmetricCryptoSecuritySetting]]
===== Create Symmetric Crypto Security Setting

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `createSymmetricCryptoSecuritySetting(cryptoCardTransactionManagerFactory: SymmetricCryptoCardTransactionManagerFactory) -> SymmetricCryptoSecuritySetting`
| Since       | 2.0
| Description | Returns a new <<type_SymmetricCryptoSecuritySetting,`SymmetricCryptoSecuritySetting`>>.
| Parameters  | `cryptoCardTransactionManagerFactory` -- the factory of the crypto card transaction manager to be used.
| Returns     | A <<type_SymmetricCryptoSecuritySetting,`SymmetricCryptoSecuritySetting`>>.
| Throws      | `IllegalArgumentException` -- if the factory is invalid.
|===

[[type_CalypsoCardSelectionExtension]]
==== Calypso Card Selection Extension

[cols="1h,4", grid=rows, frame=all]
|===
| Kind     | Interface
| Namespace  | `calypso.card.card`
| Extends  | `CardSelectionExtension` (<<ref_cna_tr,CNA-TR-API>>)
| Since    | 2.0
| Purpose  | Enriches the <<ref_cna_tr,CNA-TR-API>> selection scenario with Calypso-specific commands and pre-opening of a secure session.
|===

By default the selection rejects PRIME revision 3 cards that have been *invalidated*.
The application MUST call
`acceptInvalidatedCard` to stop ignoring them.
For earlier revisions, the application MUST handle invalidation itself (typically via a *Select File* on the DF).

For all `prepare*` operations, unless otherwise specified, the following parameter ranges apply:

[cols="1,3", options="header"]
|===
| Parameter           | Range
| SFI                 | `[0..30]` (`0` indicates the current EF)
| Record number       | `[1..250]`
| Counter number      | `[1..83]`
| Counter value       | `[0..16777215]`
| Offset              | `[0..249]`, or `[0..32767]` for binary files
| Input data length   | `[1..250]`, or `[1..32767]` for binary files
|===

===== Accept Invalidated Card

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `acceptInvalidatedCard() -> CalypsoCardSelectionExtension`
| Since       | 1.0
| Description | Requests to accept invalidated cards during the selection stage. *Operational only on PRIME revision 3+.*
| Returns     | A `CalypsoCardSelectionExtension`.
| Throws      | None.
|===

[[op_CalypsoCardSelectionExtension_prepareGetData]]
===== Prepare Get Data

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `prepareGetData(tag: GetDataTag) -> CalypsoCardSelectionExtension`
| Since       | 1.0
| Description | Adds a *Get Data* command for the supplied tag. Useful e.g. for OMAPI cases where the FCI is not provided by *Select Application*.
| Returns     | A `CalypsoCardSelectionExtension`.
|===

[[op_CalypsoCardSelectionExtension_preparePreOpenSecureSession]]
===== Prepare Pre Open Secure Session

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `preparePreOpenSecureSession(writeAccessLevel: WriteAccessLevel) -> CalypsoCardSelectionExtension`
| Since       | 1.7
| Description
| Adds a specific *Open Secure Session* command attempting a *pre-opening*. Enables a future single-exchange session execution by anticipating the APDU responses. The mechanism is *ineffective* if (a) the card or crypto module does not support extended mode, (b) the session needs intermediate exchanges (PIN, SV, encryption, early mutual auth, unread session data), (c) the session opens with a different access level, (d) an intermediate `processCommands(...)` is called, or (e) asymmetric cryptography is used.
| Returns     | A `CalypsoCardSelectionExtension`.
| Throws      | `IllegalStateException` if pre-open is already prepared.
|===

[[op_CalypsoCardSelectionExtension_prepareReadBinary]]
===== Prepare Read Binary

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `prepareReadBinary(sfi: byte, offset: integer, nbBytesToRead: integer) -> CalypsoCardSelectionExtension`
| Since       | 1.7
| Description | Adds one or more *Read Binary* commands for a binary EF. Best-effort.
| Returns     | A `CalypsoCardSelectionExtension`.
| Throws      | None.
|===

[[op_CalypsoCardSelectionExtension_prepareReadCounter]]
===== Prepare Read Counter

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `prepareReadCounter(sfi: byte, nbCountersToRead: integer) -> CalypsoCardSelectionExtension`
| Since       | 1.7
| Description | Adds a *Read Records* command reading the counter record up to the supplied counter number (all preceding counters are also read). Best-effort.
| Returns     | A `CalypsoCardSelectionExtension`.
| Throws      | None.
|===

[[op_CalypsoCardSelectionExtension_prepareReadRecord]]
===== Prepare Read Record

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `prepareReadRecord(sfi: byte, recordNumber: integer) -> CalypsoCardSelectionExtension`
| Since       | 1.1
| Description | Adds a *Read Records* command for a single record of a linear or cyclic EF. Best-effort: the command does not fail if the file/record is absent.
| Returns     | A `CalypsoCardSelectionExtension`.
| Throws      | None.
|===

[[op_CalypsoCardSelectionExtension_prepareSelectFile_byLid]]
===== Prepare Select File (LID)

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `prepareSelectFile(lid: short) -> CalypsoCardSelectionExtension`
| Since       | 1.0
| Description | Adds a *Select File* command targeting an EF by its LID. Caution: PRIME 3 compatible only. The command fails if the selected file is not an EF.
| Returns     | A `CalypsoCardSelectionExtension`.
| Throws      | None.
|===

[[op_CalypsoCardSelectionExtension_prepareSelectFile_byControl]]
===== Prepare Select File (Select Control)

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `prepareSelectFile(selectControl: SelectFileControl) -> CalypsoCardSelectionExtension`
| Since       | 1.0
| Description | Adds a *Select File* command using a navigation control (`FIRST`, `NEXT` or `CURRENT`).
| Returns     | A `CalypsoCardSelectionExtension`.
|===

[[type_DirectoryHeader]]
==== Directory Header

[cols="1h,4", grid=rows, frame=all]
|===
| Kind     | Interface
| Namespace  | `calypso.card.card`
| Since    | 1.0
| Purpose  | Carries the metadata of a Calypso DF.
|===

===== Get Access Conditions

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `getAccessConditions() -> byte array`
| Since       | 1.0
| Description | Returns a reference to the access conditions.
| Returns     | A `byte array`.
| Throws      | None.
|===

===== Get DF Status

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `getDfStatus() -> byte`
| Since       | 1.0
| Description | Returns the DF status byte.
| Returns     | A `byte` value.
| Throws      | None.
|===

===== Get Key Indexes

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `getKeyIndexes() -> byte array`
| Since       | 1.0
| Description | Returns a reference to the keys indexes.
| Returns     | A `byte array`.
| Throws      | None.
|===

[[op_DirectoryHeader_getKif]]
===== Get KIF

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `getKif(writeAccessLevel: WriteAccessLevel) -> byte`
| Since       | 1.0
| Description | Returns the KIF associated with the supplied write access level.
| Returns     | A `byte` value.
|===

[[op_DirectoryHeader_getKvc]]
===== Get KVC

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `getKvc(writeAccessLevel: WriteAccessLevel) -> byte`
| Since       | 1.0
| Description | Returns the KVC associated with the supplied write access level.
| Returns     | A `byte` value.
|===

===== Get LID

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `getLid() -> short`
| Since       | 1.0
| Description | Returns the associated LID.
| Returns     | A `short` value.
| Throws      | None.
|===

[[type_ElementaryFile]]
==== Elementary File

[cols="1h,4", grid=rows, frame=all]
|===
| Kind     | Interface
| Namespace  | `calypso.card.card`
| Since    | 1.0
| Purpose  | Calypso Elementary File. Carries an `SFI`, a `FileHeader` and a `FileData`.
|===

===== Get Data

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `getData() -> FileData`
| Since       | 1.0
| Description | Returns the file data.
| Returns     | A `FileData`.
| Throws      | None.
|===

===== Get Header

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `getHeader() -> FileHeader?`
| Since       | 1.0
| Description | Returns the file header, or `null` if not yet set.
| Returns     | A `FileHeader`.
| Throws      | None.
|===

===== Get SFI

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `getSfi() -> byte`
| Since       | 1.0
| Description | Returns the SFI.
| Returns     | A `byte` value.
| Throws      | None.
|===

[[type_FileData]]
==== File Data

[cols="1h,4", grid=rows, frame=all]
|===
| Kind     | Interface
| Namespace  | `calypso.card.card`
| Since    | 1.0
| Purpose  | Carries the content of a Calypso EF and exposes counter-oriented accessors.
|===

===== Get All Counters Value

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `getAllCountersValue() -> sortedMap<integer, integer>`
| Since       | 1.0
| Description | Returns the values of all counters extracted from record #1.
| Returns     | A `sortedMap<integer, integer>`.
| Throws      | None.
|===

===== Get All Records Content

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `getAllRecordsContent() -> sortedMap<integer, byte array>`
| Since       | 1.0
| Description | Returns the known records content.
| Returns     | A `sortedMap<integer, byte array>`.
| Throws      | None.
|===

===== Get Content (No Args)

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `getContent() -> byte array`
| Since       | 1.0
| Description | Returns the known content of record #1 (or the entire content for a binary file).
| Returns     | A `byte array`.
| Throws      | None.
|===

[[op_FileData_getContent_byRecord]]
===== Get Content (Num Record)

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `getContent(numRecord: integer) -> byte array`
| Since       | 1.0
| Description | Returns the known content of the supplied record number.
| Returns     | A `byte array`.
| Throws      | None.
|===

[[op_FileData_getContent_byRange]]
===== Get Content (Num Record, Data Offset, Data Length)

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `getContent(numRecord: integer, dataOffset: integer, dataLength: integer) -> byte array`
| Since       | 1.0
| Description | Returns a copy of the known content subset of a record from `dataOffset` to `dataOffset + dataLength`.
| Returns     | A `byte array`.
| Throws      | `IllegalArgumentException` -- if `dataOffset < 0` or `dataLength < 1`. + `IndexOutOfBoundsException` -- if the range exceeds the record content.
|===

[[op_FileData_getContentAsCounterValue]]
===== Get Content As Counter Value

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `getContentAsCounterValue(numCounter: integer) -> integer?`
| Since       | 1.0
| Description | Returns the value of counter `numCounter`. The counter value is extracted from the 3-byte slice at offset `(numCounter - 1) * 3` of record #1. Returns `null` if record #1 or the requested counter is not set.
| Returns     | An `integer` value, or `null` if record #1 or the requested counter is not set.
| Throws      | `IllegalArgumentException` -- if `numCounter < 1`. + `IndexOutOfBoundsException` -- if the counter is truncated.
|===

[[type_FileHeader]]
==== File Header

[cols="1h,4", grid=rows, frame=all]
|===
| Kind     | Interface
| Namespace  | `calypso.card.card`
| Since    | 1.0
| Purpose  | Carries the metadata of a Calypso EF.
|===

===== Get Access Conditions

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `getAccessConditions() -> byte array`
| Since       | 1.0
| Description | Returns the access conditions; empty array if not available.
| Returns     | A `byte array`.
| Throws      | None.
|===

===== Get DF Status

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `getDfStatus() -> byte?`
| Since       | 1.0
| Description | Returns the DF status, or `null` if not available (e.g. when the header is built from the response to a *Get Data* with `EF_LIST` tag).
| Returns     | A `byte` value, or `null` if not available.
| Throws      | None.
|===

===== Get EF Type

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `getEfType() -> ElementaryFile.Type`
| Since       | 1.0
| Description | Returns the EF type.
| Returns     | A `ElementaryFile.Type`.
| Throws      | None.
|===

===== Get Key Indexes

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `getKeyIndexes() -> byte array`
| Since       | 1.0
| Description | Returns the key indexes; empty array if not available.
| Returns     | A `byte array`.
| Throws      | None.
|===

===== Get LID

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `getLid() -> short`
| Since       | 1.0
| Description | Returns the associated LID.
| Returns     | A `short` value.
| Throws      | None.
|===

===== Get Record Size

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `getRecordSize() -> integer`
| Since       | 1.0
| Description | Returns the size of a record. For binary files, this is the size of the file.
| Returns     | An `integer` value.
| Throws      | None.
|===

===== Get Records Number

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `getRecordsNumber() -> integer`
| Since       | 1.0
| Description | Returns the number of records. For *counter* and *binary* files the value is always 1.
| Returns     | An `integer` value.
| Throws      | None.
|===

===== Get Shared Reference

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `getSharedReference() -> short?`
| Since       | 1.0
| Description | Returns the non-zero unique identifier of the shared data when the file data is shared. Returns `0` if not shared, `null` if the information is not available.
| Returns     | A `short` value (`0` if not shared), or `null` if the information is not available.
| Throws      | None.
|===

[[type_FreeTransactionManager]]
==== Free Transaction Manager

[cols="1h,4", grid=rows, frame=all]
|===
| Kind     | Interface
| Namespace  | `calypso.card.transaction`
| Extends  | <<type_TransactionManager,`TransactionManager<FreeTransactionManager>`>>
| Since    | 2.0
| Purpose  | Transaction manager that does *not* require any cryptographic computation.
|===

`FreeTransactionManager` does not add any operation.

[[type_SearchCommandData]]
==== Search Command Data

[cols="1h,4", grid=rows, frame=all]
|===
| Kind     | Interface
| Namespace  | `calypso.card.transaction`
| Since    | 1.1
| Purpose  | Carries the input/output data of `TransactionManager.prepareSearchRecords`.
|===

[[op_SearchCommandData_setSfi]]
[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `setSfi(sfi: byte) -> SearchCommandData`
| Since       | 1.1
| Description | Sets the SFI of the EF in which the search is performed.
| Returns     | A `SearchCommandData`.
| Throws      | None.
|===

[[op_SearchCommandData_startAtRecord]]
[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `startAtRecord(recordNumber: integer) -> SearchCommandData`
| Since       | 1.1
| Description | Record number where the search begins (default: 1).
| Returns     | A `SearchCommandData`.
| Throws      | None.
|===

[[op_SearchCommandData_setOffset]]
[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `setOffset(offset: integer) -> SearchCommandData`
| Since       | 1.1
| Description | Offset (in bytes) at which the analysis starts (default: 0).
| Returns     | A `SearchCommandData`.
| Throws      | None.
|===

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `enableRepeatedOffset() -> SearchCommandData`
| Since       | 1.1
| Description | Analyse the data at the supplied offset, then repeatedly at each following offset until the end of the record.
| Returns     | A `SearchCommandData`.
| Throws      | None.
|===

[[op_SearchCommandData_setSearchData]]
[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `setSearchData(data: byte array) -> SearchCommandData`
| Since       | 1.1
| Description | Sets the data to search.
| Returns     | A `SearchCommandData`.
| Throws      | None.
|===

[[op_SearchCommandData_setMask]]
[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `setMask(mask: byte array) -> SearchCommandData`
| Since       | 1.1
| Description | Sets the comparison mask. The mask length MUST be `<=` the search data length; missing bytes are right-padded with `FFh`.
| Returns     | A `SearchCommandData`.
| Throws      | None.
|===

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `fetchFirstMatchingResult() -> SearchCommandData`
| Since       | 1.1
| Description | Requests to fetch the content of the first matching record into the `CalypsoCard`.
| Returns     | A `SearchCommandData`.
| Throws      | None.
|===

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `getMatchingRecordNumbers() -> list<integer>`
| Since       | 1.1
| Description | Returns the record numbers that matched.
| Returns     | A `list<integer>`.
| Throws      | None.
|===

[[type_SecureExtendedModeTransactionManager]]
==== Secure Extended Mode Transaction Manager

[cols="1h,4", grid=rows, frame=all]
|===
| Kind     | Interface
| Namespace  | `calypso.card.transaction`
| Extends  | <<type_SecureSymmetricCryptoTransactionManager,`SecureSymmetricCryptoTransactionManager<SecureExtendedModeTransactionManager>`>>
| Since    | 2.0
| Purpose  | Symmetric-key transaction manager exposing additional operations only available on *Calypso Prime Extended* products.
|===

===== Prepare Activate Encryption

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `prepareActivateEncryption() -> SecureExtendedModeTransactionManager`
| Since       | 2.0
| Description
| Requests encryption for all following session commands. SHOULD be limited to commands that require it (resource
intensive). When mutual authentication is also required, the two `prepare*` calls SHOULD be placed
consecutively. Only makes sense in a secure session.
| Returns     | A `SecureExtendedModeTransactionManager`.
| Throws      | `UnsupportedOperationException` -- if the *Manage Secure Session* command is not available for this context (the card and/or the cryptographic module does not support the extended mode).
|===

===== Prepare Deactivate Encryption

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `prepareDeactivateEncryption() -> SecureExtendedModeTransactionManager`
| Since       | 2.0
| Description | Requests to stop the encryption. NOTE: `prepareCloseSecureSession()` automatically stops the encryption.
| Returns     | A `SecureExtendedModeTransactionManager`.
| Throws      | `UnsupportedOperationException` -- if the *Manage Secure Session* command is not available for this context (the card and/or the cryptographic module does not support the extended mode).
|===

===== Prepare Early Mutual Authentication

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `prepareEarlyMutualAuthentication() -> SecureExtendedModeTransactionManager`
| Since       | 2.0
| Description
| Requests to mutually authenticate the card and the terminal *before* the secure session is closed. The use of
this feature penalises the execution time of the secure session and SHOULD be used only when needed (e.g. before
sending sensitive commands).
| Returns     | A `SecureExtendedModeTransactionManager`.
| Throws      | `UnsupportedOperationException` -- if the *Manage Secure Session* command is not available for this context (the card and/or the cryptographic module does not support the extended mode).
|===

[[type_SecurePkiModeTransactionManager]]
==== Secure PKI Mode Transaction Manager

[cols="1h,4", grid=rows, frame=all]
|===
| Kind     | Interface
| Namespace  | `calypso.card.transaction`
| Extends  | <<type_SecureTransactionManager,`SecureTransactionManager<SecurePkiModeTransactionManager>`>>
| Since    | 2.1
| Purpose  | Asymmetric-key transaction manager, compatible with Calypso cards in *PKI mode*.
|===

===== Prepare Open Secure Session

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `prepareOpenSecureSession() -> SecurePkiModeTransactionManager`
| Since       | 2.1
| Description
| Schedules an *Open Secure Session* command in PKI mode. If the next prepared command is a *Read One Record* or
*Read One Or More Counters*, the implementation SHOULD merge it with the session opening for optimisation.
| Returns     | A `SecurePkiModeTransactionManager`.
| Throws      | None.
|===

[[type_SecureRegularModeTransactionManager]]
==== Secure Regular Mode Transaction Manager

[cols="1h,4", grid=rows, frame=all]
|===
| Kind     | Interface
| Namespace  | `calypso.card.transaction`
| Extends  | <<type_SecureSymmetricCryptoTransactionManager,`SecureSymmetricCryptoTransactionManager<SecureRegularModeTransactionManager>`>>
| Since    | 2.0
| Purpose  | Symmetric-key transaction manager compatible with every Calypso product.
|===

No additional operation declared.

[[type_SecureSessionStatus]]
==== Secure Session Status

[cols="1h,4", grid=rows, frame=all]
|===
| Kind     | Interface
| Namespace  | `calypso.card.transaction`
| Since    | 3.0
| Purpose  | Immutable snapshot of the current secure session state, returned by <<op_getSecureSessionStatus,`TransactionManager.getSecureSessionStatus`>>.
|===

A `SecureSessionStatus` is *captured* at the moment of the call and does *not* reflect subsequent changes to the session state.
To obtain a refreshed status, the caller MUST re-invoke `getSecureSessionStatus()`.

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `isOpen() -> boolean`
| Since       | 3.0
| Description | Indicates whether a secure session is open at the snapshot moment.
| Returns     | A `boolean` value.
| Throws      | None.
|===

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `getType() -> SecureSessionType`
| Since       | 3.0
| Description | Returns the cryptographic nature of the session (symmetric vs asymmetric).
| Returns     | A `SecureSessionType`.
| Throws      | None.
|===

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `getWriteAccessLevel() -> WriteAccessLevel?`
| Since       | 3.0
| Description | Returns the write access level requested at session opening. MUST return `null` in PKI mode (`SecureSessionType.ASYMMETRIC`) where session opening does not take a `WriteAccessLevel`.
| Returns     | A `WriteAccessLevel`.
| Throws      | None.
|===

[[type_SecureSymmetricCryptoTransactionManager]]
==== Secure Symmetric Crypto Transaction Manager

[cols="1h,4", grid=rows, frame=all]
|===
| Kind     | Interface
| Namespace  | `calypso.card.transaction`
| Generics | `<T extends SecureSymmetricCryptoTransactionManager<T>>`
| Extends  | <<type_SecureTransactionManager,`SecureTransactionManager<T>`>>
| Since    | 2.0
| Purpose  | Common operations for every transaction manager backed by *symmetric* cryptography.
|===

[[op_SecureSymmetricCryptoTransactionManager_prepareChangeKey]]
===== Prepare Change Key

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `prepareChangeKey(keyIndex: integer, newKif: byte, newKvc: byte, issuerKif: byte, issuerKvc: byte) -> T`
| Since       | 2.0
| Description
| Schedules a *Change Key* command. *MUST be performed outside a secure session.* All KIFs/KVCs MUST be available
in the crypto module.
| Parameters
| `keyIndex`  -- the index of the key to be replaced (`1` for the issuer key, `2` for the load key, `3` for the debit key). +
`newKif`    -- the KIF of the new key. +
`newKvc`    -- the KVC of the new key. +
`issuerKif` -- the KIF of the current card's issuer key. +
`issuerKvc` -- the KVC of the current card's issuer key.
| Returns     | The current instance (`T`).
| Throws
| `UnsupportedOperationException` -- if the *Change Key* command is not available for this card. +
`IllegalArgumentException` -- if the provided key index is out of range. +
`IllegalStateException` -- if the command is executed while a secure session is open.
|===

===== Prepare Invalidate

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `prepareInvalidate() -> T`
| Since       | 2.0
| Description | Schedules an *Invalidate* command.
| Returns     | The current instance (`T`).
| Throws
| `IllegalStateException` -- if the card is already invalidated. +
`SessionBufferOverflowException` -- if the command would overflow the modifications buffer and the multiple-session mode is not allowed.
|===

[[op_SecureSymmetricCryptoTransactionManager_prepareOpenSecureSession]]
===== Prepare Open Secure Session

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `prepareOpenSecureSession(writeAccessLevel: WriteAccessLevel) -> T`
| Since       | 2.0
| Description
| Schedules an *Open Secure Session* command. If the next prepared command is a *Read One Record* or *Read One Or
More Counters*, the implementation SHOULD merge it with the session opening for optimisation, unless the
pre-open mode is active or the read-merge optimisation has been disabled.
| Parameters  | `writeAccessLevel` -- the write access level to be used.
| Returns     | The current instance (`T`).
| Throws
| `IllegalStateException` -- in one of the following cases: no `SymmetricCryptoSecuritySetting` is available, a secure session opening is already prepared, or a secure session is already opened.
|===

===== Prepare Rehabilitate

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `prepareRehabilitate() -> T`
| Since       | 2.0
| Description | Schedules a *Rehabilitate* command.
| Returns     | The current instance (`T`).
| Throws
| `IllegalStateException` -- if the card is not invalidated. +
`SessionBufferOverflowException` -- if the command would overflow the modifications buffer and the multiple-session mode is not allowed.
|===

[[op_SecureSymmetricCryptoTransactionManager_prepareSvDebit_withData]]
===== Prepare SV Debit (Date)

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `prepareSvDebit(amount: integer, date: byte array, time: byte array) -> T`
| Since       | 2.0
| Description
| Schedules an *SV Debit* or *SV Undebit* command using the supplied additional data. Amount: `0..32767` for
debit, `0..32768` for undebit.
| Parameters
| `amount` -- the amount to be subtracted (`0..32767`) or added (`0..32768`). +
`date`   -- a 2-byte free value. +
`time`   -- a 2-byte free value.
| Returns     | The current instance (`T`).
| Throws
| `IllegalArgumentException` -- if one of the provided arguments is out of range. +
`IllegalStateException` -- in one of the following cases: the new value is negative and negative balances are not allowed, another SV command was already prepared inside the same secure session, the SV command is not placed in the first position in the list of prepared commands, the SV command does not follow an *SV Get* command, or the command and the SV operation are not consistent. +
`SessionBufferOverflowException` -- if the command would overflow the modifications buffer and the multiple-session mode is not allowed.
|===

[[op_SecureSymmetricCryptoTransactionManager_prepareSvDebit_amountOnly]]
===== Prepare SV Debit (No Args)

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `prepareSvDebit(amount: integer) -> T`
| Since       | 2.0
| Description | Schedules an *SV Debit* or *SV Undebit* command with the optional data fields set to zero.
| Parameters  | `amount` -- the amount to be subtracted (`0..32767`) or added (`0..32768`).
| Returns     | The current instance (`T`).
| Throws
| `IllegalArgumentException` -- if the provided argument is out of range. +
`IllegalStateException` -- in one of the following cases: the new value is negative and negative balances are not allowed, another SV command was already prepared inside the same secure session, the SV command is not placed in the first position in the list of prepared commands, the SV command does not follow an *SV Get* command, or the command and the SV operation are not consistent. +
`SessionBufferOverflowException` -- if the command would overflow the modifications buffer and the multiple-session mode is not allowed.
|===

[[op_SecureSymmetricCryptoTransactionManager_prepareSvGet]]
===== Prepare SV Get

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `prepareSvGet(svOperation: SvOperation, svAction: SvAction) -> T`
| Since       | 2.0
| Description | Schedules an *SV Get* command.
| Parameters
| `svOperation` -- the nature of the intended operation: debit or reload. +
`svAction`    -- the type of action: `DO` (a debit or a positive reload), `UNDO` (an undebit or a negative reload).
| Returns     | The current instance (`T`).
| Throws      | `UnsupportedOperationException` -- if the SV feature is not available for this card.
|===

[[op_SecureSymmetricCryptoTransactionManager_prepareSvReload_withData]]
===== Prepare SV Reload (Date)

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `prepareSvReload(amount: integer, date: byte array, time: byte array, free: byte array) -> T`
| Since       | 2.0
| Description
| Schedules an *SV Reload* command using the supplied additional data. Amount range: `-8388608..8388607`.
| Parameters
| `amount` -- the value to be reloaded, positive or negative integer in the range `-8388608..8388607`. +
`date`   -- a 2-byte free value. +
`time`   -- a 2-byte free value. +
`free`   -- a 2-byte free value.
| Returns     | The current instance (`T`).
| Throws
| `IllegalArgumentException` -- if one of the provided arguments is out of range. +
`IllegalStateException` -- in one of the following cases: another SV command was already prepared inside the same secure session, the SV command is not placed in the first position in the list of prepared commands, the SV command does not follow an *SV Get* command, or the command and the SV operation are not consistent. +
`SessionBufferOverflowException` -- if the command would overflow the modifications buffer and the multiple-session mode is not allowed.
|===

[[op_SecureSymmetricCryptoTransactionManager_prepareSvReload_amountOnly]]
===== Prepare SV Reload (No Args)

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `prepareSvReload(amount: integer) -> T`
| Since       | 2.0
| Description | Schedules an *SV Reload* command with the optional data fields set to zero.
| Parameters  | `amount` -- the value to be reloaded, positive integer in the range `0..8388607` for a `DO` action, in the range `0..8388608` for an `UNDO` action.
| Returns     | The current instance (`T`).
| Throws
| `IllegalArgumentException` -- if the provided argument is out of range. +
`IllegalStateException` -- in one of the following cases: another SV command was already prepared inside the same secure session, the SV command is not placed in the first position in the list of prepared commands, the SV command does not follow an *SV Get* command, or the command and the SV operation are not consistent. +
`SessionBufferOverflowException` -- if the command would overflow the modifications buffer and the multiple-session mode is not allowed.
|===

[[type_SecureTransactionManager]]
==== Secure Transaction Manager

[cols="1h,4", grid=rows, frame=all]
|===
| Kind     | Interface
| Namespace  | `calypso.card.transaction`
| Generics | `<T extends SecureTransactionManager<T>>`
| Extends  | <<type_TransactionManager,`TransactionManager<T>`>>
| Since    | 2.0
| Purpose  | Common operations for every secure (cryptographically-backed) transaction manager.
|===

[[op_SecureTransactionManager_getCryptoExtension]]
===== Get Crypto Extension

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `getCryptoExtension(cryptoExtensionClass: Class<E>) -> E extends CardTransactionCryptoExtension`
| Since       | 2.0
| Description | Returns the associated <<type_CardTransactionCryptoExtension,`CardTransactionCryptoExtension`>>.
| Parameters  | `cryptoExtensionClass` -- the class of the crypto extension.
| Returns     | A `E extends CardTransactionCryptoExtension`.
|===

===== Prepare Cancel Secure Session

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `prepareCancelSecureSession() -> T`
| Since       | 2.0
| Description | Schedules a special *Close Secure Session* command aborting the current secure session. Executed in safe mode -- MUST NOT raise exceptions.
| Returns     | The current instance (`T`).
| Throws      | None.
|===

===== Prepare Close Secure Session

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `prepareCloseSecureSession() -> T`
| Since       | 2.0
| Description
| Schedules a *Close Secure Session* command. The ratification mechanism is disabled by default but MAY be enabled
via <<type_SymmetricCryptoSecuritySetting,`SymmetricCryptoSecuritySetting.enableRatificationMechanism`>>. In
this case, a ratification command MUST be added after the *Close Secure Session* command when in contactless
mode.
| Returns     | The current instance (`T`).
| Throws      | `IllegalStateException` -- in one of the following cases: no secure session is opened and no secure session opening is prepared, a secure session closing is already prepared, or a secure session cancelling is prepared.
|===

[[type_SvDebitLogRecord]]
==== SV Debit Log Record

[cols="1h,4", grid=rows, frame=all]
|===
| Kind     | Interface
| Namespace  | `calypso.card.card`
| Since    | 1.0
| Purpose  | One record of a Stored Value debit log.
|===

===== Get Amount

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `getAmount() -> integer`
| Since       | 1.0
| Description | Returns the debit amount.
| Returns     | An `integer` value.
| Throws      | None.
|===

===== Get Balance

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `getBalance() -> integer`
| Since       | 1.0
| Description | Returns the SV balance after the debit.
| Returns     | An `integer` value.
| Throws      | None.
|===

===== Get Debit Date

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `getDebitDate() -> byte array`
| Since       | 1.0
| Description | Returns the 2-byte debit date.
| Returns     | A `byte array`.
| Throws      | None.
|===

===== Get Debit Time

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `getDebitTime() -> byte array`
| Since       | 1.0
| Description | Returns the 2-byte debit time.
| Returns     | A `byte array`.
| Throws      | None.
|===

===== Get KVC

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `getKvc() -> byte`
| Since       | 1.0
| Description | Returns the KVC of the debit key.
| Returns     | A `byte` value.
| Throws      | None.
|===

===== Get Raw Data

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `getRawData() -> byte array`
| Since       | 1.0
| Description | Returns the raw bytes of the debit log record.
| Returns     | A `byte array`.
| Throws      | None.
|===

===== Get SAM ID

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `getSamId() -> byte array`
| Since       | 1.0
| Description | Returns the 4-byte SAM ID.
| Returns     | A `byte array`.
| Throws      | None.
|===

===== Get SAM T Num

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `getSamTNum() -> integer`
| Since       | 1.0
| Description | Returns the SAM transaction number.
| Returns     | An `integer` value.
| Throws      | None.
|===

===== Get SV T Num

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `getSvTNum() -> integer`
| Since       | 1.0
| Description | Returns the SV transaction number.
| Returns     | An `integer` value.
| Throws      | None.
|===

[[type_SvLoadLogRecord]]
==== SV Load Log Record

[cols="1h,4", grid=rows, frame=all]
|===
| Kind     | Interface
| Namespace  | `calypso.card.card`
| Since    | 1.0
| Purpose  | One record of a Stored Value load log.
|===

===== Get Amount

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `getAmount() -> integer`
| Since       | 1.0
| Description | Returns the load amount.
| Returns     | An `integer` value.
| Throws      | None.
|===

===== Get Balance

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `getBalance() -> integer`
| Since       | 1.0
| Description | Returns the SV balance after the load.
| Returns     | An `integer` value.
| Throws      | None.
|===

===== Get Free Data

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `getFreeData() -> byte array`
| Since       | 1.0
| Description | Returns the 2-byte free data.
| Returns     | A `byte array`.
| Throws      | None.
|===

===== Get KVC

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `getKvc() -> byte`
| Since       | 1.0
| Description | Returns the KVC of the load key.
| Returns     | A `byte` value.
| Throws      | None.
|===

===== Get Load Date

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `getLoadDate() -> byte array`
| Since       | 1.0
| Description | Returns the 2-byte load date.
| Returns     | A `byte array`.
| Throws      | None.
|===

===== Get Load Time

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `getLoadTime() -> byte array`
| Since       | 1.0
| Description | Returns the 2-byte load time.
| Returns     | A `byte array`.
| Throws      | None.
|===

===== Get Raw Data

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `getRawData() -> byte array`
| Since       | 1.0
| Description | Returns the raw bytes of the load log record.
| Returns     | A `byte array`.
| Throws      | None.
|===

===== Get SAM ID

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `getSamId() -> byte array`
| Since       | 1.0
| Description | Returns the 4-byte SAM ID.
| Returns     | A `byte array`.
| Throws      | None.
|===

===== Get SAM T Num

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `getSamTNum() -> integer`
| Since       | 1.0
| Description | Returns the SAM transaction number.
| Returns     | An `integer` value.
| Throws      | None.
|===

===== Get SV T Num

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `getSvTNum() -> integer`
| Since       | 1.0
| Description | Returns the SV transaction number.
| Returns     | An `integer` value.
| Throws      | None.
|===

[[type_SymmetricCryptoSecuritySetting]]
==== Symmetric Crypto Security Setting

[cols="1h,4", grid=rows, frame=all]
|===
| Kind     | Interface
| Namespace  | `calypso.card.transaction`
| Since    | 2.0
| Purpose  | Security setting carrier for symmetric-key Calypso card transactions (e.g. SAM-backed).
|===

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `enableMultipleSession() -> SymmetricCryptoSecuritySetting`
| Since       | 2.0
| Description | Enables multiple-session mode to handle more changes than the session buffer allows.
| Returns     | A `SymmetricCryptoSecuritySetting`.
| Throws      | None.
|===

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `enableRatificationMechanism() -> SymmetricCryptoSecuritySetting`
| Since       | 2.0
| Description | Enables the ratification mechanism to handle early card removal preventing reception of the closing ACK.
| Returns     | A `SymmetricCryptoSecuritySetting`.
| Throws      | None.
|===

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `enablePinPlainTransmission() -> SymmetricCryptoSecuritySetting`
| Since       | 2.0
| Description | Enables the PIN transmission in plain text.
| Returns     | A `SymmetricCryptoSecuritySetting`.
| Throws      | None.
|===

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `enableSvLoadAndDebitLog() -> SymmetricCryptoSecuritySetting`
| Since       | 2.0
| Description | Enables retrieval of both load *and* debit log records. Default: disabled.
| Returns     | A `SymmetricCryptoSecuritySetting`.
| Throws      | None.
|===

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `authorizeSvNegativeBalance() -> SymmetricCryptoSecuritySetting`
| Since       | 2.0
| Description | Allows the SV balance to become negative. Default: disabled.
| Returns     | A `SymmetricCryptoSecuritySetting`.
| Throws      | None.
|===

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `disableReadOnSessionOpening() -> SymmetricCryptoSecuritySetting`
| Since       | 2.0
| Description | Disables the automatic merging of *Open Secure Session* with a possible *Read Record* command. By default, the optimisation is performed.
| Returns     | A `SymmetricCryptoSecuritySetting`.
| Throws      | None.
|===

[[op_SymmetricCryptoSecuritySetting_assignKif]]
[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `assignKif(writeAccessLevel: WriteAccessLevel, kvc: byte, kif: byte) -> SymmetricCryptoSecuritySetting`
| Since       | 2.0
| Description | Defines, for a write access level, the KIF to use when the card only provides a KVC.
| Returns     | A `SymmetricCryptoSecuritySetting`.
| Throws      | None.
|===

[[op_SymmetricCryptoSecuritySetting_assignDefaultKif]]
[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `assignDefaultKif(writeAccessLevel: WriteAccessLevel, kif: byte) -> SymmetricCryptoSecuritySetting`
| Since       | 2.0
| Description | Defines the default KIF for a write access level.
| Returns     | A `SymmetricCryptoSecuritySetting`.
| Throws      | None.
|===

[[op_SymmetricCryptoSecuritySetting_assignDefaultKvc]]
[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `assignDefaultKvc(writeAccessLevel: WriteAccessLevel, kvc: byte) -> SymmetricCryptoSecuritySetting`
| Since       | 2.0
| Description | Defines the default KVC for a write access level.
| Returns     | A `SymmetricCryptoSecuritySetting`.
| Throws      | None.
|===

[[op_SymmetricCryptoSecuritySetting_addAuthorizedSessionKey]]
[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `addAuthorizedSessionKey(kif: byte, kvc: byte) -> SymmetricCryptoSecuritySetting`
| Since       | 2.0
| Description | Adds an authorised session key. Default: every key accepted; once at least one key is added, only listed keys are accepted.
| Returns     | A `SymmetricCryptoSecuritySetting`.
| Throws      | None.
|===

[[op_SymmetricCryptoSecuritySetting_addAuthorizedSvKey]]
[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `addAuthorizedSvKey(kif: byte, kvc: byte) -> SymmetricCryptoSecuritySetting`
| Since       | 2.0
| Description | Adds an authorised SV key.
| Returns     | A `SymmetricCryptoSecuritySetting`.
| Throws      | None.
|===

[[op_SymmetricCryptoSecuritySetting_setPinVerificationCipheringKey]]
[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `setPinVerificationCipheringKey(kif: byte, kvc: byte) -> SymmetricCryptoSecuritySetting`
| Since       | 2.0
| Description | Sets the KIF/KVC of the PIN verification ciphering key. Default: 0/0.
| Returns     | A `SymmetricCryptoSecuritySetting`.
| Throws      | None.
|===

[[op_SymmetricCryptoSecuritySetting_setPinModificationCipheringKey]]
[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `setPinModificationCipheringKey(kif: byte, kvc: byte) -> SymmetricCryptoSecuritySetting`
| Since       | 2.0
| Description | Sets the KIF/KVC of the PIN modification ciphering key. Default: 0/0.
| Returns     | A `SymmetricCryptoSecuritySetting`.
| Throws      | None.
|===

[[op_SymmetricCryptoSecuritySetting_assignOpenSecureSessionMaxDuration_byDf]]
[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `assignOpenSecureSessionMaxDuration(csnMin: long, dfName: byte array, maxDuration: long) -> SymmetricCryptoSecuritySetting`
| Since       | 3.0
| Description | Sets the maximum duration (in milliseconds) of an open secure session for cards whose CSN is >= `csnMin` and whose application DF matches `dfName`. See <<sec_csnmin>>.
| Returns     | A `SymmetricCryptoSecuritySetting`.
| Throws      | None.
|===

[[op_SymmetricCryptoSecuritySetting_assignOpenSecureSessionMaxDuration_allDf]]
[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `assignOpenSecureSessionMaxDuration(csnMin: long, maxDuration: long) -> SymmetricCryptoSecuritySetting`
| Since       | 3.0
| Description | Same as above, applied to every DF.
| Returns     | A `SymmetricCryptoSecuritySetting`.
| Throws      | None.
|===

[[op_SymmetricCryptoSecuritySetting_assignSvOperationMaxDuration_byDf]]
[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `assignSvOperationMaxDuration(csnMin: long, dfName: byte array, maxDuration: long) -> SymmetricCryptoSecuritySetting`
| Since       | 3.0
| Description | Sets the maximum duration (in milliseconds) of a Stored Value operation for cards whose CSN is >= `csnMin` and whose application DF matches `dfName`.
| Returns     | A `SymmetricCryptoSecuritySetting`.
| Throws      | None.
|===

[[op_SymmetricCryptoSecuritySetting_assignSvOperationMaxDuration_allDf]]
[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `assignSvOperationMaxDuration(csnMin: long, maxDuration: long) -> SymmetricCryptoSecuritySetting`
| Since       | 3.0
| Description | Same as above, applied to every DF.
| Returns     | A `SymmetricCryptoSecuritySetting`.
| Throws      | None.
|===

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `initCryptoContextForNextTransaction() -> void`
| Since       | 2.0
| Description | Prepares the crypto module for the next transaction by anticipating security context configuration. MUST be called *after* the user-visible notification (e.g. after opening the gate).
| Throws
| `CryptoException` -- if an error occurred when computing a crypto operation. +
`CryptoIOException` -- if a communication error with the crypto module occurred (e.g. timeout with the reader or the computing unit, network error, etc.).
|===

[[sec_csnmin]]
[NOTE]
====
*`csnMin` combination rule.* When several `assignOpenSecureSessionMaxDuration(...)` or
`assignSvOperationMaxDuration(...)` calls are made with different `csnMin` values, each call defines a *range*
bounded by its `csnMin` and the immediately higher declared `csnMin` (or `+∞` for the highest threshold).
For a given card, the range to which its CSN belongs determines the applied bound.
This mechanism allows the integrator to introduce or tighten duration bounds progressively as new generations of cards (with higher CSNs) are issued.
====

[[type_TransactionManager]]
==== Transaction Manager

[cols="1h,4", grid=rows, frame=all]
|===
| Kind     | Interface
| Namespace  | `calypso.card.transaction`
| Generics | `<T extends TransactionManager<T>>`
| Extends  | `IsoCardTransactionManager` (<<ref_cna_tr,CNA-TR-API>>)
| Since    | 2.0
| Purpose  | Common operations for every Calypso card transaction. Prepare/process model. The stereotype on `IsoCardTransactionManager` grants access to the multi-channel cast `asMultichannelCardTransactionManager()` defined by <<ref_cna_tr,CNA-TR-API>> when the underlying ISO/IEC 7816-4 card supports multi-channel.
|===

`TransactionManager` defines the full *prepare* command surface (file selection, reads, writes, counters, PIN, asymmetric key-pair generation).
Implementations MUST update the bound `CalypsoCard` after each successful APDU exchange and MUST validate parameter ranges as documented in <<type_CalypsoCardSelectionExtension>>.

===== Get Secure Session Status

[[op_getSecureSessionStatus]]
[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `getSecureSessionStatus() -> SecureSessionStatus`
| Since       | 3.0
| Description
| Returns a <<type_SecureSessionStatus,`SecureSessionStatus`>> snapshot of the current secure session
state. The returned object is immutable and captures the state at the moment of the call.
| Returns     | A `SecureSessionStatus`.
| Throws      | None.
|===

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `getTransactionAuditData() -> list<byte array>`
| Since       | 1.2
| Description | Returns the audit data of the transaction (every APDU exchange with the card and the crypto module).
| Returns     | A `list<byte array>`.
| Throws      | None.
|===

===== Prepare Append Record

[[op_TransactionManager_prepareAppendRecord]]
[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `prepareAppendRecord(sfi: byte, recordData: byte array) -> T`
| Since       | 1.0
| Description | Adds an *Append Record* command (a new record is added, the oldest record is deleted in a cyclic file).
| Parameters
| `sfi`        -- the SFI to select. +
`recordData` -- the new record data to write.
| Returns     | The current instance (`T`).
| Throws      | None.
|===

[[op_TransactionManager_prepareUpdateRecord]]
[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `prepareUpdateRecord(sfi: byte, recordNumber: integer, recordData: byte array) -> T`
| Since       | 1.0
| Description | Adds an *Update Record* command. Bytes beyond `recordData.length` MUST be left unchanged.
| Parameters
| `sfi`          -- the SFI to select. +
`recordNumber` -- the record to update. +
`recordData`   -- the new record data. If length is less than the record size, bytes beyond length are left unchanged.
| Returns     | The current instance (`T`).
| Throws      | None.
|===

[[op_TransactionManager_prepareWriteRecord]]
[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `prepareWriteRecord(sfi: byte, recordNumber: integer, recordData: byte array) -> T`
| Since       | 1.0
| Description | Adds a *Write Record* command (binary OR with the existing data).
| Parameters
| `sfi`          -- the SFI to select. +
`recordNumber` -- the record to write. +
`recordData`   -- the data to overwrite in the record. If length is less than the record size, bytes beyond length are left unchanged.
| Returns     | The current instance (`T`).
| Throws      | None.
|===

[[op_TransactionManager_prepareUpdateBinary]]
[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `prepareUpdateBinary(sfi: byte, offset: integer, data: byte array) -> T`
| Since       | 1.1
| Description | Adds one or more *Update Binary* commands.
| Parameters
| `sfi`    -- the SFI of the EF to select. +
`offset` -- the offset (`0` indicates the first byte). +
`data`   -- the new data.
| Returns     | The current instance (`T`).
| Throws      | `UnsupportedOperationException` -- if this command is not supported by this card.
|===

[[op_TransactionManager_prepareWriteBinary]]
[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `prepareWriteBinary(sfi: byte, offset: integer, data: byte array) -> T`
| Since       | 1.1
| Description | Adds one or more *Write Binary* commands (binary OR).
| Parameters
| `sfi`    -- the SFI of the EF to select. +
`offset` -- the offset (`0` indicates the first byte). +
`data`   -- the data to write over the existing data.
| Returns     | The current instance (`T`).
| Throws      | None.
|===

[[op_TransactionManager_prepareIncreaseCounter]]
[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `prepareIncreaseCounter(sfi: byte, counterNumber: integer, incValue: integer) -> T`
| Since       | 1.0
| Description | Adds an *Increase* command. For multiple counters of the same file, `prepareIncreaseCounters` is RECOMMENDED.
| Parameters
| `sfi`           -- SFI of the EF to select. +
`counterNumber` -- the number of the counter (MUST be zero in case of a simulated counter). +
`incValue`      -- value to add to the counter (positive integer `<= 16777215`).
| Returns     | The current instance (`T`).
| Throws      | None.
|===

[[op_TransactionManager_prepareIncreaseCounters]]
[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `prepareIncreaseCounters(sfi: byte, counterNumberToIncValueMap: map<integer, integer>) -> T`
| Since       | 1.1
| Description | Adds an *Increase Multiple* command, or multiple *Increase* commands, depending on the card.
| Parameters
| `sfi`                       -- SFI of the EF to select. +
`counterNumberToIncValueMap` -- the map containing the counter numbers to be incremented and their associated increment values.
| Returns     | The current instance (`T`).
| Throws      | None.
|===

[[op_TransactionManager_prepareDecreaseCounter]]
[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `prepareDecreaseCounter(sfi: byte, counterNumber: integer, decValue: integer) -> T`
| Since       | 1.0
| Description | Adds a *Decrease* command.
| Parameters
| `sfi`           -- SFI of the EF to select. +
`counterNumber` -- the number of the counter (MUST be zero in case of a simulated counter). +
`decValue`      -- value to subtract from the counter (positive integer `<= 16777215`).
| Returns     | The current instance (`T`).
| Throws      | None.
|===

[[op_TransactionManager_prepareDecreaseCounters]]
[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `prepareDecreaseCounters(sfi: byte, counterNumberToDecValueMap: map<integer, integer>) -> T`
| Since       | 1.1
| Description | Adds a *Decrease Multiple* command or multiple *Decrease* commands.
| Parameters
| `sfi`                       -- SFI of the EF to select. +
`counterNumberToDecValueMap` -- the map containing the counter numbers to be decremented and their associated decrement values.
| Returns     | The current instance (`T`).
| Throws      | None.
|===

[[op_TransactionManager_prepareSetCounter]]
[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `prepareSetCounter(sfi: byte, counterNumber: integer, newValue: integer) -> T`
| Since       | 1.0
| Description | Adds either an *Increase* or a *Decrease* command to set the counter to the supplied value.
| Parameters
| `sfi`           -- SFI of the EF to select. +
`counterNumber` -- the counter number (`>= 1` for a counters file, `0` for a simulated "counter" file). +
`newValue`      -- the desired value for the counter (positive integer `<= 16777215`).
| Returns     | The current instance (`T`).
| Throws      | `IllegalStateException` -- if the current counter value is unknown.
|===

NOTE: Every write operation MAY raise <<type_SessionBufferOverflowException,`SessionBufferOverflowException`>> if the command would overflow the modification buffer and the multiple-session mode is not allowed.

===== Prepare Read Record

[[op_TransactionManager_prepareReadRecord]]
[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `prepareReadRecord(sfi: byte, recordNumber: integer) -> T`
| Since       | 1.1
| Description | Adds a *Read Records* command for a single record. *MUST NOT be used within a secure session; use `prepareReadRecords` instead for the in-session case.*
| Parameters
| `sfi`           -- the SFI of the EF to read. +
`recordNumber`  -- the record to read.
| Returns     | The current instance (`T`).
| Throws      | None.
|===

[[op_TransactionManager_prepareReadRecords]]
[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `prepareReadRecords(sfi: byte, fromRecordNumber: integer, toRecordNumber: integer, recordSize: integer) -> T`
| Since       | 1.1
| Description | Adds a *Read Records* command for one or more records. Best-effort outside a secure session; strict mode inside a secure session.
| Parameters
| `sfi`              -- the SFI of the EF. +
`fromRecordNumber` -- the number of the first record to read. +
`toRecordNumber`   -- the number of the last record to read. +
`recordSize`       -- the record length.
| Returns     | The current instance (`T`).
| Throws      | None.
|===

[[op_TransactionManager_prepareReadRecordsPartially]]
[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `prepareReadRecordsPartially(sfi: byte, fromRecordNumber: integer, toRecordNumber: integer, offset: integer, nbBytesToRead: integer) -> T`
| Since       | 1.1
| Description | Adds *Read Record Multiple* commands. *MUST NOT be used within a secure session.*
| Parameters
| `sfi`              -- the SFI of the EF. +
`fromRecordNumber` -- the number of the first record to read. +
`toRecordNumber`   -- the number of the last record to read. +
`offset`           -- the offset in the records where to start reading (`0` indicates the first byte). +
`nbBytesToRead`    -- the number of bytes to read from each record.
| Returns     | The current instance (`T`).
| Throws
| `UnsupportedOperationException` -- if this command is not supported by this card. +
`IllegalArgumentException` -- if one of the provided arguments is out of range. +
`IllegalStateException` -- if a secure session is open.
|===

[[op_TransactionManager_prepareReadBinary]]
[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `prepareReadBinary(sfi: byte, offset: integer, nbBytesToRead: integer) -> T`
| Since       | 1.1
| Description | Adds *Read Binary* commands. Best-effort outside a session; strict inside.
| Parameters
| `sfi`           -- the SFI of the EF. +
`offset`        -- the offset (`0` indicates the first byte). +
`nbBytesToRead` -- the number of bytes to read.
| Returns     | The current instance (`T`).
| Throws      | None.
|===

[[op_TransactionManager_prepareReadCounter]]
[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `prepareReadCounter(sfi: byte, nbCountersToRead: integer) -> T`
| Since       | 1.1
| Description | Adds a *Read Records* command targeting the supplied counter (and all preceding counters).
| Parameters
| `sfi`              -- the SFI of the EF. +
`nbCountersToRead` -- the number of counters to read.
| Returns     | The current instance (`T`).
| Throws      | None.
|===

[[op_TransactionManager_prepareSearchRecords]]
[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `prepareSearchRecords(data: SearchCommandData) -> T`
| Since       | 1.1
| Description | Adds a *Search Record Multiple* command. *MUST NOT be used within a secure session.*
| Parameters  | `data` -- the input/output data containing the parameters of the command.
| Returns     | The current instance (`T`).
| Throws
| `UnsupportedOperationException` -- if the *Search Record Multiple* command is not available for this card. +
`IllegalArgumentException` -- if the input data is inconsistent. +
`IllegalStateException` -- if a secure session is open.
|===

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `prepareCheckPinStatus() -> T`
| Since       | 1.0
| Description | Adds a *Verify PIN* command without PIN presentation in order to read the attempt counter.
| Returns     | The current instance (`T`).
| Throws      | `UnsupportedOperationException` -- if the PIN feature is not available for this card.
|===

===== Prepare Select File

[[op_TransactionManager_prepareSelectFile_byLid]]
[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `prepareSelectFile(lid: short) -> T`
| Since       | 1.1
| Description | Adds a *Select File* command targeting an EF by LID. _Fails if the file is not an EF._
| Parameters  | `lid` -- the LID of the EF to select.
| Returns     | The current instance (`T`).
| Throws      | None.
|===

[[op_TransactionManager_prepareSelectFile_byControl]]
[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `prepareSelectFile(selectFileControl: SelectFileControl) -> T`
| Since       | 1.0
| Description | Adds a *Select File* command using a navigation control.
| Parameters  | `selectFileControl` -- a <<type_SelectFileControl,`SelectFileControl`>> enum entry.
| Returns     | The current instance (`T`).
|===

[[op_TransactionManager_prepareGetData]]
[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `prepareGetData(tag: GetDataTag) -> T`
| Since       | 1.0
| Description | Adds one or more *Get Data* commands. *SECURITY WARNING: MUST NOT be used within a secure session.*
| Parameters  | `tag` -- the data type to retrieve.
| Returns     | The current instance (`T`).
| Throws
| `UnsupportedOperationException` -- if the *Get Data* command with the provided tag is not supported. +
`IllegalStateException` -- if a secure session is open.
|===

[[op_TransactionManager_preparePutData]]
[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `preparePutData(tag: PutDataTag, data: byte array) -> T`
| Since       | 2.1
| Description | Adds one or more *Put Data* commands. *MUST be invoked outside a secure session.*
| Parameters
| `tag`  -- the data type to inject. +
`data` -- the data to inject.
| Returns     | The current instance (`T`).
| Throws
| `UnsupportedOperationException` -- if the *Put Data* command with the provided tag is not supported. +
`IllegalArgumentException` -- if `data` is empty. +
`IllegalStateException` -- if a secure session is open.
|===

===== Prepare SV Read All Logs

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `prepareSvReadAllLogs() -> T`
| Since       | 1.0
| Description | Reads every SV log (load + debit).
| Returns     | The current instance (`T`).
| Throws      | `UnsupportedOperationException` -- if the SV feature is not available for this card.
|===

[[op_TransactionManager_prepareVerifyPin]]
[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `prepareVerifyPin(pin: byte array) -> T`
| Since       | 1.6
| Description | Adds a *Verify PIN* command. Plain or ciphered transmission depends on `SymmetricCryptoSecuritySetting`.
| Parameters  | `pin` -- the PIN code value (4-byte long `byte array`).
| Returns     | The current instance (`T`).
| Throws
| `UnsupportedOperationException` -- if the PIN feature is not available for this card. +
`IllegalArgumentException` -- if the provided argument is out of range.
|===

[[op_TransactionManager_prepareChangePin]]
[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `prepareChangePin(newPin: byte array) -> T`
| Since       | 1.6
| Description | Adds a *Change PIN* command. *MUST be performed outside a secure session.* If transmitted plain, the command MUST be preceded by a successful *Verify PIN*.
| Parameters  | `newPin` -- the new PIN code value (4-byte long `byte array`).
| Returns     | The current instance (`T`).
| Throws
| `UnsupportedOperationException` -- if the PIN feature is not available for this card. +
`IllegalArgumentException` -- if the provided argument is out of range. +
`IllegalStateException` -- if the command is executed while a secure session is open.
|===

[cols="1h,4", grid=rows, frame=all]
|===
| Signature   | `prepareGenerateAsymmetricKeyPair() -> T`
| Since       | 2.1
| Description | Adds a *Generate Asymmetric Key Pair* command. The public part can be retrieved via `prepareGetData(GetDataTag.CARD_PUBLIC_KEY)`.
| Returns     | The current instance (`T`).
| Throws      | None.
|===

===== Processing Operations

Processing of prepared commands is inherited from `CardTransactionManager` (<<ref_cna_tr,CNA-TR-API>>) via the
`IsoCardTransactionManager` stereotype.
The Calypso `TransactionManager` does *not* redefine `processCommands()`; it relies on the inherited contract.
Multi-channel operations (`processCommandsAndCloseChannel`, `closeChannel`) are reached on demand through `asMultichannelCardTransactionManager()` of <<ref_cna_tr,CNA-TR-API>> when the underlying card supports multi-channel.

=== SPI Interfaces

[[type_AsymmetricCryptoCardTransactionManagerFactory]]
==== Asymmetric Crypto Card Transaction Manager Factory

[cols="1h,4", grid=rows, frame=all]
|===
| Kind     | Marker interface (SPI)
| Namespace  | `calypso.card.transaction.spi`
| Since    | 2.0
| Purpose  | Marker provided by crypto extensions to secure Calypso card transactions with *asymmetric* keys (PKI). Declares no operation.
|===

[[type_CardTransactionCryptoExtension]]
==== Card Transaction Crypto Extension

[cols="1h,4", grid=rows, frame=all]
|===
| Kind     | Marker interface (SPI)
| Namespace  | `calypso.card.transaction.spi`
| Since    | 2.0
| Purpose  | Marker enriching the card transaction command set with specific crypto commands (e.g. signature computation/verification). Declares no operation at the API level.
|===

[[type_PcaCertificate]]
==== PCA Certificate

[cols="1h,4", grid=rows, frame=all]
|===
| Kind     | Marker interfaces (SPI)
| Namespace  | `calypso.card.transaction.spi`
| Since    | 2.1
| Purpose  | Markers for PCA, CA and card certificates exposed by <<ref_cna_tcca,CNA-TCCA-API>> extensions.
|===

`PcaCertificate`, `CaCertificate` and `CardCertificate` declare no operation at the API level.
Their concrete content is exposed through the <<ref_cna_tcca,CNA-TCCA-API>> extension that produced them.

[[type_PcaCertificateParser]]
==== PCA Certificate Parser

[cols="1h,4", grid=rows, frame=all]
|===
| Kind     | Marker interfaces (SPI)
| Namespace  | `calypso.card.transaction.spi`
| Since    | 2.1
| Purpose  | Markers for CA and card certificate parsers exposed by <<ref_cna_tcca,CNA-TCCA-API>> extensions. Used by `AsymmetricCryptoSecuritySetting.addCaCertificateParser` / `addCardCertificateParser`.
|===

[[type_SymmetricCryptoCardTransactionManagerFactory]]
==== Symmetric Crypto Card Transaction Manager Factory

[cols="1h,4", grid=rows, frame=all]
|===
| Kind     | Marker interface (SPI)
| Namespace  | `calypso.card.transaction.spi`
| Since    | 2.0
| Purpose  | Marker provided by crypto extensions to secure Calypso card transactions with *symmetric* keys (e.g. SAM). Declares no operation.
|===

=== Enumerations

==== Calypso Card Product Type

Nested inside `CalypsoCard`. _Since 1.0._

[cols="1,1,4", options="header"]
|===
| Value               | Since | Description
| `PRIME_REVISION_1`  | 1.0 | Calypso Prime revision 1.x.
| `PRIME_REVISION_2`  | 1.0 | Calypso Prime revision 2.x.
| `PRIME_REVISION_3`  | 1.0 | Calypso Prime revision 3.x.
| `LIGHT`             | 1.0 | Calypso Light.
| `BASIC`             | 1.0 | Calypso Basic.
| `UNKNOWN`           | 1.0 | Application Type is `0` or `FFh`, or selection data could not be properly parsed.
|===

==== Elementary File Type

Nested inside `ElementaryFile`. _Since 1.0._

[cols="1,1,4", options="header"]
|===
| Value                | Since | Description
| `LINEAR`             | 1.0 | Linear EF (1 to several records).
| `BINARY`             | 1.0 | Binary EF (single continuous sequence of data bytes).
| `CYCLIC`             | 1.0 | Cyclic EF (records organised in a cycle, from the most recent to the oldest).
| `COUNTERS`           | 1.0 | Counters EF (single record containing K counters of three bytes each).
| `SIMULATED_COUNTERS` | 1.0 | Simulated counter EF (linear file with a single record, kept for Calypso Rev 2 compatibility).
|===

[[type_GetDataTag]]
==== Get Data Tag

[cols="1h,4", grid=rows, frame=all]
|===
| Kind     | Enumeration
| Namespace  | `calypso.card`
| Since    | 1.0
| Purpose  | Output data tags retrievable through the *Get Data* command. MAY NOT be applicable to all products.
|===

[cols="1,1,4", options="header"]
|===
| Value                       | Since | Description
| `FCP_FOR_CURRENT_FILE`      | 1.0 | FCP for the current file, as returned by *Select File*.
| `FCI_FOR_CURRENT_DF`        | 1.0 | FCI for the current DF, as returned by *Select Application*.
| `EF_LIST`                   | 1.1 | List of EFs in the current DF.
| `TRACEABILITY_INFORMATION`  | 1.1 | Product traceability information.
| `CARD_PUBLIC_KEY`           | 2.1 | Card public key.
| `CARD_CERTIFICATE`          | 2.1 | Card certificate.
| `CA_CERTIFICATE`            | 2.1 | CA certificate.
|===

[[type_PutDataTag]]
==== Put Data Tag

[cols="1h,4", grid=rows, frame=all]
|===
| Kind     | Enumeration
| Namespace  | `calypso.card`
| Since    | 2.1
| Purpose  | Input data tags injectable through the *Put Data* command. MAY NOT be applicable to all products.
|===

[cols="1,1,4", options="header"]
|===
| Value             | Since | Description
| `CARD_KEY_PAIR`   | 2.1 | Card key pair.
| `CARD_CERTIFICATE`| 2.1 | Card certificate.
| `CA_CERTIFICATE`  | 2.1 | CA certificate.
|===

[[type_SecureSessionType]]
==== Secure Session Type

[cols="1h,4", grid=rows, frame=all]
|===
| Kind     | Enumeration
| Namespace  | `calypso.card.transaction`
| Since    | 3.0
| Purpose  | Cryptographic nature of a Calypso secure session.
|===

[cols="1,1,4", options="header"]
|===
| Value        | Since | Description
| `SYMMETRIC`  | 3.0 | PSO/SAM sessions, Regular and Extended modes.
| `ASYMMETRIC` | 3.0 | PKI mode.
|===

NOTE: This enumeration reflects the *cryptographic nature* of the session and not the *application mode*
(Regular / Extended).
Both symmetric modes share the same underlying cryptography and only differ in their configuration at the `TransactionManager` level; the integrator who needs to know the application mode obtains it directly via the sub-type of `TransactionManager` that has been instantiated.

[[type_SelectFileControl]]
==== Select File Control

[cols="1h,4", grid=rows, frame=all]
|===
| Kind     | Enumeration
| Namespace  | `calypso.card`
| Since    | 1.0
| Purpose  | Expected behaviour of the *Select File* command (cf. ISO/IEC 7816-4 and Calypso specifications).
|===

[cols="1,1,4", options="header"]
|===
| Value         | Since | Description
| `FIRST_EF`    | 1.0 | The first EF of the current Calypso DF.
| `NEXT_EF`     | 1.0 | The next EF of the current Calypso DF.
| `CURRENT_DF`  | 1.0 | The current Calypso DF.
|===

[[type_SvAction]]
==== SV Action

[cols="1h,4", grid=rows, frame=all]
|===
| Kind     | Enumeration
| Namespace  | `calypso.card.transaction`
| Since    | 1.0
| Purpose  | Direction of an SV action.
|===

[cols="1,1,4", options="header"]
|===
| Value  | Since | Description
| `DO`   | 1.0 | In a `RELOAD`, load a positive amount; in a `DEBIT`, debit a positive amount.
| `UNDO` | 1.0 | In a `RELOAD`, load a negative amount; in a `DEBIT`, cancel (totally or partially) a previous debit.
|===

[[type_SvOperation]]
==== SV Operation

[cols="1h,4", grid=rows, frame=all]
|===
| Kind     | Enumeration
| Namespace  | `calypso.card.transaction`
| Since    | 1.0
| Purpose  | Type of Stored Value operation.
|===

[cols="1,1,4", options="header"]
|===
| Value     | Since | Description
| `RELOAD`  | 1.0 | Increase the SV balance.
| `DEBIT`   | 1.0 | Decrease the SV balance.
|===

[[type_WriteAccessLevel]]
==== Write Access Level

[cols="1h,4", grid=rows, frame=all]
|===
| Kind     | Enumeration
| Namespace  | `calypso.card`
| Since    | 1.0
| Purpose  | Write access level for a Calypso secure session. Each level induces the use of a different session-key role.
|===

[cols="1,1,4", options="header"]
|===
| Value              | Since | Description
| `PERSONALIZATION`  | 1.0 | Personalisation, load and debit operations -- uses the *issuer* key.
| `LOAD`             | 1.0 | Load and debit operations -- uses the *load* key.
| `DEBIT`            | 1.0 | Debit operations only -- uses the *debit* key.
|===

[[sec_exceptions]]
=== Exceptions

[[type_CardSignatureNotVerifiableException]]
==== Card Signature Not Verifiable Exception

[cols="1h,4", grid=rows, frame=all]
|===
| Kind     | Runtime exception
| Namespace  | `calypso.card.transaction`
| Since    | 1.2
| Purpose  | Indicates that the card has correctly closed the secure session but that the authenticity of the session cannot be verified because the crypto module is no longer available (timeout, network problem, etc.).
|===

[[type_CryptoException]]
==== Crypto Exception

[cols="1h,4", grid=rows, frame=all]
|===
| Kind     | Runtime exception
| Namespace  | `calypso.card.transaction`
| Since    | 2.0
| Purpose  | Indicates that an error occurred when computing a crypto operation.
|===

[[type_CryptoIOException]]
==== Crypto IO Exception

[cols="1h,4", grid=rows, frame=all]
|===
| Kind     | Runtime exception
| Namespace  | `calypso.card.transaction`
| Since    | 2.0
| Purpose  | Indicates a communication error with the crypto module.
|===

[[type_InconsistentDataException]]
==== Inconsistent Data Exception

[cols="1h,4", grid=rows, frame=all]
|===
| Kind     | Runtime exception
| Namespace  | `calypso.card.transaction`
| Since    | 1.2
| Purpose  | Indicates inconsistent data: either APDU-exchange de-synchronisation, or a discrepancy between data read in session vs out of session.
|===

[[type_InvalidCardSignatureException]]
==== Invalid Card Signature Exception

[cols="1h,4", grid=rows, frame=all]
|===
| Kind     | Runtime exception
| Namespace  | `calypso.card.transaction`
| Since    | 1.2
| Purpose  | Indicates that the card signature is incorrect. For symmetric-key transactions, this typically happens when the *Digest Authenticate* or *SV Check* status word is `6988h`. For PKI transactions, this simply means that the card signature is invalid.
|===

[[type_InvalidCertificateException]]
==== Invalid Certificate Exception

[cols="1h,4", grid=rows, frame=all]
|===
| Kind     | Runtime exception
| Namespace  | `calypso.card.transaction`
| Since    | 2.1
| Purpose  | Indicates that a certificate failed validation (signature, validity period, issuer or subject details, constraints, extensions, expiration, revocation).
|===

[[type_InvalidPinException]]
==== Invalid PIN Exception

[cols="1h,4", grid=rows, frame=all]
|===
| Kind     | Runtime exception
| Namespace  | `calypso.card.transaction`
| Since    | 2.0
| Purpose  | Indicates that the provided PIN is invalid.
|===

[[type_SelectFileException]]
==== Select File Exception

[cols="1h,4", grid=rows, frame=all]
|===
| Kind     | Runtime exception
| Namespace  | `calypso.card.transaction`
| Since    | 1.4
| Purpose  | Indicates that file selection failed because the file was not found (status word `6A82h`).
|===

[[type_SessionBufferOverflowException]]
==== Session Buffer Overflow Exception

[cols="1h,4", grid=rows, frame=all]
|===
| Kind     | Runtime exception
| Namespace  | `calypso.card.transaction`
| Since    | 1.2
| Purpose  | Indicates that the secure session cannot be performed atomically because the modification buffer capacity is insufficient.
|===

[[type_UnauthorizedKeyException]]
==== Unauthorized Key Exception

[cols="1h,4", grid=rows, frame=all]
|===
| Kind     | Runtime exception
| Namespace  | `calypso.card.transaction`
| Since    | 1.0
| Purpose  | Indicates that the card requires an unauthorised session key.
|===

[[sec_behaviours]]
== Behavioural Requirements

. The `CalypsoCard` instance bound to a transaction MUST be a *dynamic view* of the card image and MUST be updated after every successful command.
Failed write commands MUST NOT update the in-memory image.
. Operations marked with a *SECURITY WARNING* MUST NOT be used inside a secure session; implementations MUST raise `IllegalStateException` if invoked while a session is open.
. The merging of *Open Secure Session* with a possible *Read Record* command is enabled by default; the application MAY opt out via `SymmetricCryptoSecuritySetting.disableReadOnSessionOpening()`.
When the pre-open mode is active, this optimisation MUST NOT be performed.
. The *PKI* mode (`SecurePkiModeTransactionManager`) MUST NOT be available unless the card declares
`isPkiModeSupported() == true`.
. Communication errors at the reader, card or APDU level MUST be reported through the <<ref_cna_tr,CNA-TR-API>> exceptions (`ReaderCommunicationException`, `CardCommunicationException`, `InvalidCardResponseException`); this specification does not duplicate them.
. Implementations MUST raise `SessionBufferOverflowException` instead of silently truncating a session when multiple-session mode is disabled and the modification buffer would overflow.

// =============================================================================
[appendix]
== Copyright and License

Copyright (c) {organization}, https://calypsonet.org.

This specification is distributed under the terms of the *Eclipse Public License -- v 2.0* (EPL-2.0). See https://www.eclipse.org/legal/epl-2.0/ for the license text.
